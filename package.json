{
  "name": "Spark-Kicks",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js --ignore client",
    "develop": "concurrently \"cd server/server.js && npm run start\" \"cd client && npm start\"",
    "install": "cd server/server.js && npm i && cd ../client && npm i",
    "seed": "cd server/server.js && npm run seed",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
